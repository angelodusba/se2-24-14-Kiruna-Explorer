class Scale{
    id: number;
    name: string;

    constructor(id: number, name: string) {
        this.id = id;
        this.name = name;
      }
}
export default Scale;

//! in create document, we should update the create document to take scale_id as a combo box (similar to type)??
